version: '3.4'

services:
  uinversity:
    image: ${DOCKER_REGISTRY-}uinversity
    build:
      context: .
      dockerfile: Uinversity/Dockerfile
    ports: 
    - "5000:80"
    depends_on:
      - cache
    links:
      - cache

  cache:
    container_name: redis
    image: redis